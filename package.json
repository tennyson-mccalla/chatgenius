{
  "name": "chatgenius",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "yarn workspace frontend start",
    "build": "yarn workspace frontend build"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "firebase": "^11.1.0"
  }
}
